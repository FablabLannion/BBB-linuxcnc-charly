to install these components, use the cmd :
comp --install nameofthecomponent.comp


to initialise it with the 3 channel,  in an halrun environement use the cmd :
 loadrt nameofthecomp count=3 personnality=1,2,3

